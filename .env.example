# Vercel AI SDK uses this env var name by default
GOOGLE_GENERATIVE_AI_API_KEY=your-gemini-api-key-here

# Allowed origins for API security (comma-separated)
# Required in production to prevent unauthorized API access
# Example: https://spelling-words.vercel.app,https://myapp.com
# Supports wildcards: *.vercel.app
# For local dev, defaults to localhost:3000 if not set
ALLOWED_ORIGINS=https://your-app.vercel.app
